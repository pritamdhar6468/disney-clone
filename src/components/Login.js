import styled from "styled-components";

const Login= (props)=>{
    return (
        <Container>
            <div>yarn baba</div>
        </Container>
    )
};
const Container=styled.section`
overflow:hidden;
display;
`;
export default Login;